Quick project review. Use subagents for all investigation.

Use subagents to:
1. Check all HTML files for broken internal links
2. Verify SVG theme variants have matching dimensions
3. Confirm bcode_lang.js grammar covers all documented syntax
4. Check hljs-theme.css colors match site design tokens

Report findings as a concise checklist. Do NOT read files in main context.
