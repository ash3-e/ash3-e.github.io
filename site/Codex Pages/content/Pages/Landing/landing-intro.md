This landing page is built around a reusable 1x2 slider module. The image preview advances automatically every minute, the text stays latched to the active image, and the same base component powers the rest of the site.

Below it, the hero card introduces the three subsidiary pages in a way that mirrors the structure in your reference PNGs while keeping the final implementation original.
