This page embeds the current PDF stored in `content/Pages/Resume/Resume.pdf`.

The floppy-disk button below downloads the exact same file, so the visible document and the downloadable document always stay in sync.
