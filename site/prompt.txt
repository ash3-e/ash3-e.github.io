Create this as a parallel implementation, not a replacement. Save all new or modified outputs under a new agent-specific filename prefix using your own agent name, and do not overwrite any existing project files. For example, if you are Codex, write agent-prefixed variants like codex-index.html, codex-site.js, codex-styles.css, codex-bcode-doc-nav.js, or similarly scoped agent-prefixed files as needed. Keep all edits isolated to your own prefixed files and update references only within your own agent-prefixed implementation.

Before finishing, verify that you did not overwrite, rename, delete, or mutate the current shared implementation files. Your deliverable must be fully isolated in agent-prefixed files so it can run side-by-side with other agent implementations from Codex, Claude Code, and Gemini Antigravity without file conflicts. Assume all three agents are working simultaneously in the same folder and must not touch each other’s outputs.
Build the interactive documentation sitemap tree for the BCODe docs portal as a production-quality website feature. This is not a mockup, not a detached prototype, and not a redesign of the portal. The goal is to implement a live interactive tree map inside the current documentation portal page, using the saved final SVG text file in the project folder as the canonical visual and structural reference, while using the existing HTML, CSS, and JavaScript files as the behavioral and content-model source of truth. The final result should look and behave like the updated portal draft, but inherit the useful hover-preview and jump behavior from the old beta portal.

The single most important visual reference for the tree is the saved SVG text file in the project folder named bcode-doc-nav-final.svg. Use that file as the authoritative source for the rendered tree’s composition, card layout, connector routing, node order, spacing relationships, active Example Gallery treatment, protocol divider, and overall architecture-style composition. Do not use the most recent ad hoc SVG or any other intermediate SVG as the authority. Use bcode-doc-nav-final.svg specifically. Treat it as the source of truth for how the sitemap should be laid out and styled. The attached PNGs of the old and updated portals are visual behavior references, but the tree’s exact structure should be implemented from the final SVG text reference.

At the same time, do not build this as a static SVG image drop-in. The sitemap tree must be implemented as live HTML, CSS, and JavaScript so that it integrates with the existing documentation portal and behaves like a real navigation surface. The tree should be styled to visually match the SVG, but it should be composed of real interactive DOM elements, not a dead embedded graphic. The user must be able to hover cards, hover subtitles, click titles, click subtitles, expand and collapse Example Gallery, search across the documentation, see glowing matching nodes, preview content in the bottom preview card, and jump directly into documentation pages and anchored sections.

Use the current project files as the implementation context. Reference index.html for the portal structure and where the interactive tree should live. Reference styles.css for the site’s existing design system, glow behaviors, color choices, shadows, typography, and theme handling, especially dark or balanced mode glow behavior that should inform node glow and active-state treatment. Reference site.js for the current documentation data model, page-type behavior, search concepts, reader link patterns, quick-jump behavior, subtopic anchors, and portal metadata. Reuse and extend those systems rather than inventing a separate parallel implementation. This must feel like an extension of the current portal codebase, not an isolated feature bolted on from outside.

Also reference the reader HTML files currently in the project folder for actual document availability and for determining where title clicks and subtitle clicks should go. These files include intro-reader.html, syntax-reader.html, interpretation-reader.html, meta-v9-reader.html, meta-library-semantics-reader.html, rest-reader.html, best-practices-reader.html, telemetry-guide-reader.html, and any other active reader pages in the current docs set. If a page exists and is part of the active docs system, clicking its title in the sitemap should jump to the beginning of that page. If a page or section does not exist yet, keep the element visually present but do not force a broken jump. Preserve hover-preview behavior even when no jump target exists yet.

The portal currently has two strong reference states that need to be conceptually combined. The first is the older beta documentation portal, where hovering or selecting things loaded a larger documentation preview card lower on the page, above the footer. The second is the updated draft portal, where the architectural sitemap/tree occupies the main visual space. The goal is to merge those into one coherent feature: the sitemap tree becomes the primary interactive navigation surface, while the bottom card becomes the live preview surface for whatever node or subitem is currently being hovered or actively selected. The updated portal layout remains, but the old preview-card utility returns in a more deliberate integrated way.

Do not flatten the implementation into the old simple grid of documentation cards. Do not revert to a plain list of docs. The new tree/sitemap is the main navigation surface. The bottom card is not a tooltip and not an afterthought. It is a substantial preview surface that should feel like a continuation of the old portal design. The tree should be placed inside the documentation portal body in the same general location shown in the updated draft portal image, and the bottom preview card should appear below that tree and above the footer, occupying the role shown in the older beta portal reference. The whole page should still preserve the existing Documentation hero/search region at the top and the footer navigation at the bottom.

Use the final SVG text file to match the tree’s structure exactly as closely as practical. The layout should preserve the architecture-like shape that the SVG defines. Protocol representation nodes belong on the left side of the dashed separator. The central vertical chain includes Intro at the bottom feeding up into Syntax, then Interpretation, then the BCODe.meta Convention node. BCODe.rest and BCODe.node sit above the meta node with their connector stems aligned to their horizontal centers. Meta Library Semantics belongs off to the right side as in the final SVG. FREESTYLE belongs on the right branch from Interpretation. Best Practices belongs on the lower right. The protocol nodes and dashed divider zone must visually read as a distinct left-side cluster. The whole sitemap should feel like a technical architecture diagram rendered with FlowMapp-like stacked node cards.

Preserve the card styling language of the final SVG. Each main node should visually resemble the cards in the SVG and not generic website boxes. That means a white body, pale lavender header band, purple or indigo outline, subtle drop shadow, thin divider lines between header and body, tightly controlled spacing, monospace-leaning or technical-feeling typography for the tree labels, and consistent connector strokes matching the SVG palette. The final tree should visually feel like the SVG has come alive in the page. The colors, shadows, active glow, dashed divider, and connector language should be directly derived from the design notes inside bcode-doc-nav-final.svg and from the current theme system in styles.css.

Use the existing documentation metadata as the text/content authority wherever possible. Reuse DOC_PORTAL_META or its equivalent current metadata object in site.js for the summary text and subheaders for the active documents. Reuse existing reader mapping and slug logic. Reuse current subtopic anchor logic where it already exists. Do not invent a parallel hardcoded doc registry if the current site.js already exposes the relevant structures. However, the sitemap layout itself should follow the final SVG, even if the old portal grid did not arrange documents that way. In other words, use site.js as the content map and bcode-doc-nav-final.svg as the layout/styling map.

The active main documentation nodes in the sitemap should reflect the current intended docs, not stale legacy pages that are no longer part of the portal experience. Include Intro, Syntax, Interpretation, BCODe.meta Convention, Meta Library Semantics, BCODe.rest, Best Practices, Telemetry Guide if it is still part of the active portal model, the protocol representation nodes on the left, and FREESTYLE as shown in the SVG. Do not surface meta-v7 as a sitemap node. Do not surface Chat Summary as a sitemap node. If stale files remain on disk, ignore them for the interactive tree unless they are explicitly still part of the live docs portal. The sitemap should represent the active documentation architecture, not every leftover file in the folder.

Implement the tree as a hierarchy of interactive cards and subrows. Each main card represents a document or conceptual node. Within cards that have meaningful subheaders, render those subheaders as live interactive subrows or subtitle rows inside the card, matching the stacked style shown in the SVG rather than generic chips. The card headers and subtitle rows should feel like an architectural/FlowMapp hybrid: compact, orderly, clear, and readable.

The titles of the main rectangular nodes must be clickable when the destination page exists. Clicking a title should jump directly to the beginning of that documentation page. The jump target should be the canonical documentation page or reader page used by the current portal logic, not a guessed URL. Use the existing mapping logic in site.js and/or the current HTML structure to determine the correct destination. Preserve current link patterns rather than inventing new ones. If a title corresponds to a node that is conceptual or not backed by a current live page, keep the title non-broken and non-jumping for now while still supporting hover-preview behavior.

The subtitles within node cards must also be interactive. Clicking a subtitle should jump directly to the relevant position within the target documentation. This should use the same anchor/jump mechanics already present in the old documentation portal where subtitle chips and quick-jump links navigate into sections of the docs. Reuse the current anchor semantics in site.js wherever possible. If an anchor already exists for a subtitle, use it exactly. If a reader file exists but a clean anchor is missing, preserve progressive enhancement and do not hard-fail the interaction; favor landing at the page start if necessary over creating a broken fragment link.

Now implement hover behavior carefully. When the user mouses over any main rectangular node, the bottom documentation preview card should load that node’s preview. This includes the document title, any useful preview metadata such as its filename or label when that exists in the current portal, the subtitle list if relevant, and the summary/description/body preview text. The bottom card should resemble the older beta portal’s preview card behavior and styling. It should not feel like a tooltip. It should occupy a stable region below the tree and update fluidly as the user hovers different nodes.

If the hovered node corresponds to a real document with known summary and quick-jump metadata, populate the bottom preview card with meaningful content. If the hovered node corresponds to a conceptual or not-yet-backed page, still populate the preview card with at least the title and leave the body area blank or minimal instead of inventing fake content. The preview card should always feel intentional and useful even when content is incomplete. If a node lacks full data, show that cleanly rather than cluttering the card with placeholders.

In addition to loading preview content on hover, hovering a main rectangular node should visually highlight that node. This highlight should not replace the default design language but should reinforce focus. When a node is hovered, highlight the node header text and the subtitle rows within that node so the user can immediately see what card is currently active. The hover highlight should affect both the title/header region and the subtitles/subrows inside the hovered node. This is important. The entire node should feel activated, not just the outer card border. The user explicitly wants the subtitles and headers within the rectangular elements to highlight when each of those elements is moused over.

That highlight behavior must also apply at the subtitle level. If the user mouses over a specific subtitle row inside a node, highlight that subtitle row clearly, and also keep the parent node visually active. The subtitle row should feel like a focused interactive target. The bottom preview card should then load content appropriate to that specific subtitle rather than just the document’s general summary. For example, hovering Architecture under Intro should preview the Architecture section of Intro. Hovering Atomic latching under Intro should preview that section. Hovering a subrow inside Syntax or Interpretation should preview that specific subsection if the target content exists. If the subsection content is not yet extracted or available, the preview can fall back to the document title plus the subtitle label and minimal surrounding context.

For Example Gallery, do the same. When the Example Gallery tree is expanded, hovering a specific Example Gallery subtitle item should highlight that item, keep the Intro card visually active, and update the bottom preview card with the corresponding example section content if available. If a full preview cannot yet be assembled for a given example, at minimum show the Intro title, the Example Gallery grouping, and the hovered example label. The user explicitly wants subtitle highlighting to apply for the Example Gallery subtitles as well.

Search behavior must be tightly integrated with this tree. Use the current documentation search bar near the top of the page as the primary search input. Do not create a second search box for the tree. Reuse the current docs search input and its existing styling. When the search query matches content within one or more documentation pages, the corresponding node or nodes in the sitemap must glow. This glow should use the portal’s established glow language from styles.css and the SVG’s active-state language. The entire rectangular node element should glow when that document contains a matching search result. Do not only glow the title text. Do not only glow a subtitle chip. The whole node should be visually elevated so matching documents are obvious at a glance.

If the search query yields matches in a document that also has specific matching subtitles or sections, preserve as much specificity as possible. The whole node should glow, but within the node, the matching subtitle/subrow should also be highlighted so the user can tell where in that node the search hit is coming from. If multiple subtitles inside the same node match, visually indicate that more than one row is relevant without making the card unreadable. The node glow is the first-level signal; subtitle emphasis is the second-level signal.

When a glowing search-result node is hovered, the bottom preview card should not merely show the node’s generic summary. Instead, it should load a result-aware preview that shows a useful contextual snippet around the matched text. This should behave like a richer version of the old preview card. Use the same documentation corpus that the current portal search uses. If site.js already has snippet or search-context behavior, reuse it. If not, implement a search-snippet extraction that is consistent with the portal’s style. The preview should make it obvious why the node matched the search and where the result lives inside the doc.

When a glowing node is clicked, it should jump directly to the matched search result within the target document if that document exists. The jump should preserve enough query or anchor state so the destination can show or focus that result consistently. Likewise, if the bottom preview card is currently showing a result-aware preview because of a hovered glowing node, then clicking the bottom preview card should also jump directly to that matched search result in the document. This should feel like a deliberate dual-navigation model: the tree points you to candidate docs, the preview card gives you context, and clicking either can take you directly to the relevant place.

If multiple documents match the search, multiple nodes may glow simultaneously. Hover behavior should still work independently. Hovering one glowing node should load that node’s specific result preview into the bottom card without interfering with the others. If a node is glowing because of search but the user hovers a different non-glowing node, the preview card should follow the hovered node. Hover state should temporarily take precedence over passive search state in the preview card. However, the search glow should remain visible until the query is cleared or changed.

Default load behavior is important. On initial page load, the sitemap should render without the Example Gallery list expanded. The Intro node should still contain the rows Architecture, Atomic latching, and Example gallery, and the Example Gallery row should be visibly active/glowing by default to match the SVG’s design language, but the large list of example items should not be shown yet. This means the Example Gallery is present as a special active row, but its subtree is collapsed on initial load.

Clicking anywhere on the glowing Example Gallery subtitle row inside the Intro node should expand the Example Gallery list. Treat the whole glowing row as an activation target, not just the small triangle. Once expanded, the triangle icon next to that row becomes the explicit open/close control for toggling the Example Gallery subtree. After expansion, clicking the triangle should collapse or re-expand the list. Preserve the exact behavioral intent here: initial click on the glowing row opens it, while later the triangle is the explicit toggle affordance. The user wants that behavior specifically.

The expanded Example Gallery list should be sourced from the current Intro documentation content and quick-jump information, not fabricated from memory. Use the Intro document as the content authority for the example entries. Keep the naming aligned with the Intro Example Gallery section. Each example item in the expanded list should be a live interactive row beneath the Intro card or integrated into the same tree zone in a way that matches the intended architecture composition. Hovering an example item should update the bottom preview card. Clicking an example item should jump directly to that example’s section in the Intro document. The hover highlight should visually affect the example row itself and indicate it is the currently previewed item.

The tree must implement the connector logic from the final SVG in a way that stays faithful to the diagram. This does not necessarily mean drawing literal SVG connector paths, but the layout and visual cues must preserve the same relationships. The central vertical stack should read clearly from Intro upward to Syntax, then Interpretation, then BCODe.meta Convention, then the branch upward to BCODe.rest and BCODe.node. The protocol representation nodes should sit left of the dashed separator. Meta Library Semantics should sit to the right of the meta node. FREESTYLE should sit on the right branch from Interpretation. Best Practices should sit in the right-lower area. The overall composition should still look like the SVG even though it is implemented with live DOM.

If you do use SVG for connectors or a hybrid overlay for arrows and dashed lines, keep it subordinate to the live DOM node system. The nodes themselves must still be real HTML elements so hover, click, focus, and search behaviors are easy to wire. A hybrid model is acceptable if it produces the closest faithful rendering, but the implementation must not degrade into a non-interactive static artboard. The connectors should visually follow the SVG logic, but the node interactivity must remain first-class.

The bottom preview card needs its own detailed behavior rules. It should load the currently hovered node or hovered subtitle content. If there is no current hover, it may reflect the most recently selected item or a default state based on the current page context. If the user has not interacted yet, the bottom preview card can default to the Intro card content or remain in a neutral state consistent with the older beta portal. When the user hovers a node, the preview card should update quickly but not flicker. When the user moves from the node into the preview card, the preview card should remain stable long enough to be clickable and readable. Avoid tooltip-like disappearance. The preview card should feel anchored and intentional.

Clicking the bottom preview card should be context-sensitive. If the preview card is showing a general document preview because of a node hover, clicking the card should jump to that document’s start. If the preview card is showing a subtitle-specific preview, clicking it should jump to that subtitle anchor in the document. If the preview card is showing a search-result-specific preview, clicking it should jump directly to that search result location. The preview card should therefore act as a second target for all currently previewed content, not just a passive display region.

Implement accessible interaction states as well. Hover is important, but keyboard focus and pointer focus should also be considered. Main nodes and subrows should be keyboard reachable if possible. Focus states should visually resemble hover states so the tree can be navigated without a mouse. The bottom preview card should still respond to focus/selection context where practical. Search-result glow and active Example Gallery states should not rely solely on color; structure and state indicators should remain readable.

The tree must preserve the site’s current theme behavior. The portal already supports different themes or appearance modes through the current CSS and JS. The sitemap tree, its glow behaviors, and the bottom preview card should respect those themes rather than introducing a second unrelated theme system. In dark or balanced modes, the glow and active emphasis should remain legible and consistent with the portal’s existing UX language. Use the existing CSS variables and theme classes from styles.css wherever possible.

Do not create a separate data-fetching system if the current portal already loads its documentation data into a client-side structure. Reuse the docs data already exposed in the portal. Reuse metadata objects and mappings already present in site.js. Where the current implementation has helper functions for normalization, search, snippet creation, anchor targeting, or reader-link cleanup, use or adapt them rather than duplicating logic in a brittle way. This feature should feel native to the portal codebase and should remain maintainable.

You may need to introduce a dedicated sitemap component or tree controller in JavaScript. That is acceptable, but it should still integrate with the current architecture. It should read from the existing docs metadata, build the node structure, manage hover/preview state, manage Example Gallery expansion state, manage search-result highlighting, and manage routing/jump behavior. Keep the implementation modular and understandable, but do not sacrifice fidelity to the SVG or the portal behaviors in favor of an oversimplified component.

Treat missing pages and missing anchors gracefully. Some nodes or subtitles may correspond to content that is not fully wired yet. In those cases, preserve the node visually, allow hover-preview where possible, and avoid broken navigation. Clicking should either no-op cleanly or fall back to the document start if that is appropriate and not misleading. The page should not throw errors or produce dead broken links because a section is not fully implemented yet.

Be precise with naming and visible labels. Use the current portal-facing labels, not outdated internal filenames, unless the portal deliberately shows a filename in a preview context. The visible tree labels should match the intended doc names users see in the current portal. The bottom preview card may include a smaller file label or internal display label if that is already part of the portal’s language, but the main tree should be user-facing and clean.

The hover-highlighting behavior inside nodes deserves special attention. When a user hovers a node card, the title/header area should receive a visible hover treatment and the subtitles within that node should also visually respond, even if only subtly, so the user senses that the whole card is active. When a user hovers a specific subtitle row, that row should get a stronger highlight than the rest, while the header remains active enough to indicate parent context. For Example Gallery, when the gallery is expanded and a specific example row is hovered, that example row should get the strongest highlight, the Example Gallery parent row should remain visibly active, and the Intro card should remain contextually active.

Search-result highlighting should layer on top of this, not replace it. For instance, if a node is glowing because it contains a search result and the user hovers it, the hover highlight of its header and subtitles should still occur within the already glowing node. Likewise, if a matching subtitle is hovered, it should still gain row-level emphasis within the glowing node. The system should be able to express both “this node matches the current search” and “this exact row is currently under the pointer or focus” at the same time.

Use the old portal’s bottom card behavior as the model for how preview content should load, but do not literally recreate the old card layout if the updated portal needs a cleaner integration. The preview card should feel stylistically compatible with the current portal but functionally equivalent to the old beta behavior: hover over something meaningful, see its preview below, click if you want to go there. This is one of the most important UX bridges between the old and new versions.

The Example Gallery special case should be implemented carefully because it is both part of the Intro card and also effectively its own expandable subtree. The collapsed Intro card must still look tidy and aligned with the SVG. The expanded Example Gallery should not destroy the page layout or feel tacked on. It should expand in a controlled way that still reads as part of the sitemap. Use the SVG and the updated draft portal image to keep the balance of the composition. The gallery list can extend in a dedicated left-lower or contextual expansion region as implied by the final visual direction, but it must still feel architecturally tied to Intro.

Do not allow the search system to forcibly auto-expand Example Gallery unless there is a clear reason to do so. Search can glow the Intro node and possibly indicate matching Example Gallery content, but preserve the default collapsed behavior until the user interacts with Example Gallery unless your implementation needs a minimal search-state expansion for usability. If you do auto-expand under search, make it deliberate, reversible, and consistent with user expectations. In the absence of a clear need, keep Example Gallery collapsed by default and under user control.

Preserve the current page’s hero area and search bar exactly or nearly exactly. This task is about enhancing the content region below it, not redesigning the portal shell. The hero title, subtitle, search field styling, theme controls, header, and footer should remain aligned with the current portal. The sitemap tree should feel like it was always meant to live inside this portal design.

If necessary, create a dedicated tree container and preview container within the docs portal content stack. The tree container should hold the live sitemap and connector overlay. The preview container should sit below it and above the footer. Keep the layout responsive enough that it remains usable on narrower screens, but prioritize faithful desktop behavior first since the SVG composition is desktop-oriented. On narrower widths, degrade gracefully rather than collapsing the architecture into nonsense.

Do not remove the existing footer documentation links. They remain useful. However, the new sitemap tree should become the primary in-page navigation and discovery surface. The footer remains secondary.

Before considering the implementation complete, verify all of these behaviors explicitly. The page loads with the interactive sitemap visible in the portal body. The tree visually matches bcode-doc-nav-final.svg as closely as practical. The bottom preview card exists below the tree. Hovering any main node loads that node’s preview into the bottom card. Hovering a subtitle loads subtitle-specific preview into the bottom card. Hovering a node highlights that node’s header and subtitles. Hovering a subtitle highlights that subtitle row and keeps its parent node active. Example Gallery is collapsed by default. The Example Gallery row glows by default. Clicking that glowing row expands the gallery. Once expanded, the triangle next to Example Gallery toggles open and close. Hovering expanded example rows highlights them and loads their preview into the bottom card. Clicking a title jumps to the page start when that page exists. Clicking a subtitle jumps to the correct in-document position when the anchor exists. Searching causes matching nodes to glow. Hovering a glowing node loads a search-result-aware preview. Clicking a glowing node or the bottom preview card jumps to the search result when possible. Stale nodes such as meta-v7 and Chat Summary do not appear in the sitemap. The finished feature feels like a production website capability, not a demo.

Finally, keep implementation choices grounded in the project files. Use bcode-doc-nav-final.svg as the visual authority. Use index.html as the page integration target. Use styles.css as the styling and glow/theme authority. Use site.js as the behavior and content-model authority. Use the reader HTML files as the real destination and content references. Use the attached old and updated portal PNGs to understand the intended UX transition: the old portal contributes the hover-preview card behavior, the updated portal contributes the main page composition, and the final SVG contributes the actual sitemap tree shape and active-state visual language. The finished feature should convincingly fuse all three into one coherent documentation portal experience.

Before finishing, verify that you did not overwrite, rename, delete, or mutate the current shared implementation files. Your deliverable must be fully isolated in agent-prefixed files so it can run side-by-side with other agent implementations from Codex, Claude Code, and Gemini Antigravity without file conflicts. Assume all three agents are working simultaneously in the same folder and must not touch each other’s outputs.